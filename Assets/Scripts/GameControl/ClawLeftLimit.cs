using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ClawLeftLimit : MonoBehaviour
{/*
    private ClawInput _player;

    private bool _left, _right;

    public bool Left => _left;
    public bool Right => _right;

    void OnTriggerEnter(Collider hitInfo)
    {
        _player = hitInfo.GetComponent<ClawInput>();

        if( _player != null )
        {
            print("collsion");
            //_left = false;

            _player.ChangeBool();
        }
    }

    void OnTriggerExit(Collider hitInfo)
    {
        _player = hitInfo.GetComponent<ClawInput>();

        if (_player != null)
        {
            print("collsion exit");
            //_left = true;

            _player.ChangeBool2();
        }
    }
    */
}
